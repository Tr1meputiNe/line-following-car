# 光电循迹小车（Arduino Uno）设计与制作

> 《Arduino 编程及应用开发》课程大作业
> 按下启动按键 → 小车从发车区出发 → 经过起始线沿黑线自主行走 1 圈（3 分钟内）→ 再次到达起始线自动停下 → OLED 显示运行时间

---

## 一、任务与评分点对照

| 评分项（PPT 第 3 页） | 分值 | 本项目的实现 |
| --- | --- | --- |
| 循迹稳定 | 10 | 两路光电对管 + 差速转向（外侧轮加速 / 内侧轮反转）+ 起步踢一脚防堵转 |
| 自动起停 | 5 | 压到起始线开始计时；再次压到起始线自动停车 |
| 显示 | 5 | OLED 大字号 `MM:SS.CC` 计时 + 3 分钟进度条 + 最终成绩画面 |
| 按键 | 5 | 启动键，外部下拉 + 30ms 简易消抖 |
| 发挥 | 5 | 蜂鸣器提示音（倒计时 / 到线 / 超时）、两个指示灯、发车失败保护、3 分钟限时保护 |
| 技术报告 | 10 | 本文档 |

---

## 二、硬件

| 部件 | 规格 |
| --- | --- |
| 主控 | Arduino Uno（ATmega328P，16MHz，2KB RAM，32KB Flash） |
| 扩展板 | Arduino 小车扩展板（板载 MX1508 双 H 桥、蜂鸣器、按键、LED、传感器接口） |
| 电机驱动 | **MX1508** 双路 H 桥，每路用 IN1 / IN2 两个脚控制（PWM 直接加在 IN 脚上） |
| 循迹传感器 | 红外光电对管 ×2，**模拟输出**，接 A0 / A1 |
| 显示屏 | 0.96" OLED，**SSD1306** 驱动，**128×64**，**I2C**，地址 0x3C |
| 电机 | 3 轮车模底盘，2 路直流减速电机 |
| 供电 | USB（**见第九节，这是本项目最大的限制**） |

---

## 三、引脚分配

**全部实测确认过，写在 `src/main.cpp` 顶部的"引脚"那一段。**

| 功能 | 引脚 | 说明 |
| --- | --- | --- |
| 左传感器 | **A0** | 模拟输入 |
| 右传感器 | **A1** | 模拟输入 |
| 左轮 前进 / 反转 | **D9 / D10** | MX1508 的 MOTOR-B（换板后实测） |
| 右轮 前进 / 反转 | **D5 / D6** | MX1508 的 MOTOR-A（换板后实测） |
| 蜂鸣器 | **D4** | 用 `tone()` 发声 |
| 启动按键 | **D7** | 板上已有外部下拉：松开 LOW，按下 HIGH |
| 运行指示灯 | **D11** | LED2 |
| 状态指示灯 | **D12** | LED1 |
| OLED SDA / SCL | **A4 / A5** | 板载 I2C |

### 3.1 两个「反了」的坑（实测记录）

**扩展板丝印标的左右和实际是反的**，接线时按丝印接会导致转向反向。实测方法和结论：

| 实测方法 | 现象 | 结论 |
| --- | --- | --- |
| `bringup` 敲 `q`（代码让"左轮"变慢），看哪个物理轮子慢 | 左边那个慢 | **MOTOR-B(D9/D10) 驱动左轮，MOTOR-A(D5/D6) 驱动右轮** |
| `bringup` 敲 `r`，手指挡住车左侧传感器，看哪列数字变 | 右列数字变 | **左侧传感器实际接在 A0** |

所以代码里按实测写死，**和丝印不一致是正常的**。

### 3.2 为什么 D5/D6 和 D9/D10 这样分

- 每个轮子的两个脚放在**同一个定时器**上（D5/D6 = Timer0，D9/D10 = Timer1），两轮的 PWM 频率一致
- D4 用 `tone()` 只占 **Timer2**，不影响电机 PWM
- 12 个引脚互不重复，零冲突

### 3.3 接线表

#### ① 先把「地」连成一片（最容易忘的一步）

所有模块的 GND 必须连通。否则会出现：读数乱跳、电机不转、屏幕不亮。

```
Arduino GND ─┬─ MX1508 / 扩展板 GND
             ├─ OLED GND
             ├─ 左传感器 GND
             ├─ 右传感器 GND
             ├─ 启动按键的一端
             ├─ 两个 LED 的负极
             ├─ 蜂鸣器负极
             └─ 电池负极（如果用电池给电机供电）
```

#### ② 光电对管（两路接线完全一样，只是输出口不同）

```
发射管 IR333：  +5V ── R1(220Ω) ── 长脚(A) ── 短脚(K) ── GND

接收管 PT334：                    ┌── 集电极 C（长脚）
                  +5V ── R2(10kΩ) ─┤
                                  └── 输出 ──> A0（左）/ A1（右）

                 发射极 E（短脚） ── GND
```

| 从 | 到 |
| --- | --- |
| 左传感器输出 | **A0** |
| 右传感器输出 | **A1** |

> ⚠️ PT334 的 C / E 别接反。接反了不会烧，但读数会一直贴在 1023 不动。
> ⚠️ 发射管和接收管之间必须夹**黑色隔光片**，否则黑白读数会一样。

#### ③ 电机驱动 MX1508

| MX1508 引脚 | 连到 | 说明 |
| --- | --- | --- |
| **VCC** | Arduino **5V** | 逻辑电源 |
| **GND** | 公共 GND | |
| **VM** | 电机电源正极 | 见本节 ⑦ |
| **IN1** | **D5** | 右轮 前进 |
| **IN2** | **D6** | 右轮 反转 |
| **IN3** | **D9** | 左轮 前进 |
| **IN4** | **D10** | 左轮 反转 |
| **MOTOR-A（AO1/AO2）** | **右**电机两根线 | 接反了就两根线对调 |
| **MOTOR-B（BO1/BO2）** | **左**电机两根线 | 同上 |

> MX1508 没有单独的 PWM 脚和 STBY 脚，**调速直接用 `analogWrite` 打在 IN 脚上**。
> 有些板子丝印写成 `A1/A2/A3/A4`，对应关系一样。

#### ④ OLED（SSD1306，I2C）

| OLED | 连到 |
| --- | --- |
| GND | 公共 GND |
| VCC | Arduino 5V（少数模块只支持 3.3V，看背面丝印） |
| SDA | **A4** |
| SCL | **A5** |

#### ⑤ 启动按键

| 从 | 到 |
| --- | --- |
| 按键 一脚 | **D7** |
| 按键 另一脚 | **GND** |

> 代码用的是 `pinMode(startButton, INPUT)`，**靠板上的外部下拉电阻**：松开读 LOW，按下读 HIGH。
> 如果板上没有下拉，改成 `INPUT_PULLUP` 并把判断反过来（用 `bringup` 的 `b` 命令可以验证）。

#### ⑥ 指示灯和蜂鸣器

| 从 | 到 |
| --- | --- |
| **D11** | 220Ω → 运行指示灯 长脚（正极） |
| 运行指示灯 短脚（负极） | GND |
| **D12** | 220Ω → 状态指示灯 长脚（正极） |
| 状态指示灯 短脚（负极） | GND |
| 蜂鸣器 正极（+） | **D4** |
| 蜂鸣器 负极（−） | GND |

> 蜂鸣器建议串一个 100Ω 限流电阻。用**无源**蜂鸣器效果最好（`tone()` 能奏出不同音高）。
> 不确定蜂鸣器接在哪个脚，用 `bringup` 的 `z` 命令扫描。

#### ⑦ 供电（**本项目最关键的一环，见第九节**）

```
【推荐接法】
电池盒 / 充电宝 ──> MX1508 的 VM
Arduino ──USB──> 电脑 / 另一个充电宝
两边 GND 必须接在一起

【当前接法（有隐患）】
Arduino 的 5V ──> MX1508 的 VM
```

> ⚠️ **实测踩过的坑**：曾经出现过"电机突然停、过一会儿又好"的现象，
> 一度误判成"USV 供电被 500mA 保险丝限流"。后来**直接量 MX1508 的 VM 脚，发现只有 1V**
> —— 说明是 **5V 到 VM 这一段接触不良/断开**，电根本没送到电机，和电流能力无关。
> **教训：怀疑供电时，一定要量"用电设备那一端"（VM），不要只量电源那一端（Uno 5V）。**

---

## 四、传感器摆法与阈值标定

### 4.1 本车是「夹住线」的摆法

黑线夹在两个传感器**中间**，所以：

| 左 | 右 | 含义 |
| --- | --- | --- |
| **白** | **白** | **车正正压在线上**（正常状态） |
| 黑 | 白 | 线跑到车左边 → 车偏右 |
| 白 | 黑 | 线跑到车右边 → 车偏左 |
| 黑 | 黑 | 起始线 |

> ⚠️ **这一点非常关键，也是踩过的坑。** 代码最初把"两个都白"当成"丢线，按上次方向继续转" —— 但本车正常行驶时就是两个白，结果车一直在拐，**根本走不了直线**。现在"两个都白"是**直行**。
> 如果哪天换成"压在线上"的摆法（两个都黑才是正），这段逻辑必须跟着改。

### 4.2 实测阈值

| 状态 | 左传感器 | 右传感器 |
| --- | --- | --- |
| 黑线 | 941 ~ 949 | 915 ~ 926 |
| 白底 | 757 ~ 762 | 705 ~ 708 |

取两组之间的中间值：

```cpp
int threshold = 840;    // 读数 > 840 算黑线
int blackHigh = 1;      // 黑线读数大
```

余量：黑线最低 915 比 840 高 75，白底最高 762 比 840 低 78。实测噪声只有 ±2，非常稳。

**怎么自己重测**：`bringup` 敲 `r`，输出会带 `B`（黑线）/ `W`（白底）标记，5 秒一轮，正好够把车从白底滑到黑线上。

### 4.3 PPT 第 13 页思考题：R1 / R2 取值

> 本车用的是扩展板，R1/R2 已在板上焊好。下面是本题的解答，作为技术报告的理论部分。

**R1（发射管 IR333 限流电阻）**

```
R1 = (VCC - VF) / IF = (5 - 1.2) / 0.02 = 190 Ω
```

取标称值 **180Ω 或 220Ω**。220Ω 时 IF ≈ 17.3mA，发热小、寿命长，推荐。

**R2（接收管 PT334 负载电阻）**

输出 V_out ≈ I_C × R2，I_C 由反射光强决定。建议 **10kΩ 起**：

| 状态 | 期望 ADC 读数 |
| --- | --- |
| 黑线 | 900 以上 |
| 白底 | 150 以下 |

两组差值不到 100 → R2 取值不合适或者隔光没做好。

**最容易翻车的点**：发射管和接收管之间必须加**黑色隔光片**。否则红外光不经地面反射、直接从内部串过去，黑白读数会一样，怎么调阈值都没用。

**机械高度**：光电对管离地 **5 ~ 10mm** 最佳。

---

## 五、控制逻辑

### 5.1 循迹（`loop()` 第四步）

```cpp
int leftBlack  = leftValue  > threshold;   // 1 = 压到黑线
int rightBlack = rightValue > threshold;
```

| 左 | 右 | 判断 | 动作 |
| --- | --- | --- | --- |
| 白 | 白 | 车正 | 两轮都 `speedFast`，**直行** |
| 黑 | 白 | 车偏右 | 左轮**反转** `speedBack`，右轮 `speedTurn` |
| 白 | 黑 | 车偏左 | 左轮 `speedTurn`，右轮**反转** `speedBack` |
| 黑 | 黑 | 起始线 | 两轮直行，并开始判圈 |

**转向为什么用"内侧轮反转"**：外侧轮往前冲 + 内侧轮往后倒，车身几乎是原地转，这是双轮差速能做出最狠的转向。

> 每段代码都把 4 个脚写全（前进脚 + 反转脚），否则上一次转弯留下的反转会被带到下一段。

### 5.2 起步「踢一脚」（`startKickMs`）

直流减速电机**静止时的阻力比转动时大得多**，直接给 `speedFast` 经常原地不转（需要手推）。所以发车时先给满速 255 冲一下：

```
倒计时 3-2-1 → 满速 255 踢 150ms → 降回 speedFast=130 循迹
```

### 5.3 计时与判圈

计时**从压到起始线的瞬间开始**，不是按启动键：

```
按启动键 → 倒计时 → 直线冲向起始线（不计时）→ 压线，计时开始
        → 沿黑线跑 1 圈 → 再次压线，停车显示成绩
```

**发车阶段**：直线冲，看到双黑就是起始线，直接开始计时（**不做延时确认** —— 车速快时 150ms 已经冲过整条线了，再检查一次反而读到白底，导致永远找不到起始线）。

**判圈**：要求双黑**连续保持 `startLineMs`** 且已跑够 `lapMinMs`，防止直角弯被误判。

> `startLineMs` 和起始线宽度直接相关：压线时间 = 线宽 ÷ 车速。
> 本车线宽 **3cm**、车速约 20~25cm/s，压线 120~150ms，所以取 **80ms** 留余量。
> **跑完一圈停不下来** → 降到 60；**弯道提前停车** → 加到 110。
> 最彻底的办法是把起始线做得比引导线**宽**（30mm 对 20mm），两者的"双黑持续时间"自然就拉开了。

---

## 六、可调参数（`src/main.cpp` 顶部）

| 参数 | 当前值 | 作用 | 怎么调 |
| --- | --- | --- | --- |
| `threshold` | 840 | 黑白分界（**读数大于它算黑线**） | 用 `bringup` 的 `r` 命令实测 |
| `blackHigh` | 1 | 黑线读数大填 1，小填 0 | 同上，看 `r` 显示 B 还是 W |
| `speedFast` | 130 | 直行速度 | 稳了再往上加 |
| `speedTurn` | 135 | 转弯外侧轮速度 | 转不够→加大；画龙→减小 |
| `speedBack` | 60 | 转弯内侧轮**反转**速度 | 转不够→加大；画龙→减小 |
| `trimLeft` | 0 | 左轮补偿（车往左偏填正数） | 每次 ±5，范围 ±30 |
| `startKickMs` | 150 | 起步满速踢多久 | 起不来→加大到 300 |
| `startLineMs` | 80 | 起始线确认时间 | 见 5.3 |
| `lapMinMs` | 5000 | 最短一圈保护 | 一般不动 |
| `lapMaxMs` | 180000 | 3 分钟限时 | PPT 要求，不动 |
| `launchMaxMs` | 5000 | 发车找线超时 | 发车区离起始线远就加大 |
| `oledMs` | 100 | 屏幕刷新间隔 | 见 7.2 |
| `debugLaunch` | 1 | 发车阶段打印读数到串口 | 排错时 1，稳定后改回 0 |
| `debugOled` | 1 | 把调试信息显示在 **OLED** 上 | 跑完停在屏幕上"验尸"，稳定后改回 0 |

> ⚠️ **Uno 上 `int` 最大只有 32767**，`180000` 装不下，所有毫秒变量必须用 `unsigned long`。

---

## 七、两个程序的关键设计

### 7.1 字符串一律用 `F()`

AVR 上普通字符串**占 RAM**，`F("...")` 把它放进 Flash。

**为什么必须这样**：128×64 的 OLED 要在运行时 `malloc` **1024 字节**显存，而 Uno 只有 2048 字节 RAM。字符串占多了 `malloc` 就会失败，`oled.begin()` 直接返回 false，**屏幕一片黑，而且编译器不会报任何警告**。

| 程序 | 加 F() 前 | 加 F() 后 | + 显存 1024 |
| --- | --- | --- | --- |
| `main.cpp` | 690 | **544** | 1568 ✅ |
| `bringup.cpp` | 1246 | **544** | 1568 ✅ |

（`bringup.cpp` 加 F() 前是 1246 + 1024 = 2270 > 2048，**OLED 从来没初始化成功过**，这个坑排查了很久。）

### 7.2 OLED 刷新间隔不能太短

刷一次屏要往 I2C 发 1024 字节，实测约 **23ms**。这 23ms 里程序卡在 `display()`，**电机收不到新指令**（控制盲区）。

100ms 刷一次 = 每 100ms 有 23ms 盲走，车按 30cm/s 算会盲走 0.8cm，**占 3cm 线宽的四分之一**。
这个值做成了可调参数 `oledMs`（当前 100）。想减少控制盲区可以调到 250，代价是屏幕刷新变慢。

---

## 八、编译与烧录

```bash
# 正式程序
pio run -e uno -t upload
pio device monitor            # 串口监视器 9600

# 分步测试程序
pio run -e bringup -t upload
```

`platformio.ini` 里的 `default_envs` 决定 VS Code 状态栏的 ✓ / → 按钮烧哪个环境，改完要重启 VS Code。

**编译结果（实测，零警告）：**

| 程序 | Flash | RAM |
| --- | --- | --- |
| `src/main.cpp` | 56.9%（18360 / 32256） | 26.6%（544 / 2048） |
| `src/bringup.cpp` | 57.1%（18420 / 32256） | 26.6%（544 / 2048） |

---

## 九、⚠️ 供电限制（本项目最大的瓶颈）

### 9.1 问题

两个直流减速电机**起步瞬间**的电流：

| 状态 | 单个电机电流（5V） |
| --- | --- |
| 空载转起来之后 | ~120mA |
| **堵转 / 刚通电还没转** | **~1000mA** |

**电机静止启动时状态就是堵转**，两个电机同时起步约 **2.2A**。

而 USB 供电路径只能给 **0.5A**（Uno 上有一颗 500mA 自恢复保险丝）。按 0.5A 算，电机端电压只有

```
0.5A × 2.25Ω ≈ 1.1V
```

**1.1V 只有额定扭矩的 20%，克服不了齿轮箱的静摩擦 —— 这就是"起步不动、推一下才走"的根本原因。**

推一下为什么有用：外力让转子转起来 → 反电动势建立 → 电流需求掉到 120mA → 供电轻松满足。

### 9.2 为什么自恢复保险丝越用越虚

polyfuse 每次跳闸，**常温阻值会永久性升高一点**。而它有个热惯性：电流刚超标的头一两百毫秒还没来得及跳闸，会放行一次超额电流 —— 我们的"踢一脚"就是在利用这个窗口。

**这个窗口很脆**：保险丝温热一点、线材换个细的、USB 口弱一点，窗口就关上了。表现出来就是"之前能跑、现在连抬起都不转"，而**等 5~10 分钟又好了**（保险丝冷却恢复）。

### 9.3 正反馈陷阱

```
电流更大 → 保险丝更烫 → 阻值更高 → 电机端电压更低
   ↑                                        ↓
   └──── 电机转不动 → 一直堵转 → 电流更大 ←──┘
```

所以**"加大力度"是错误的调参方向** —— 越加越死。本项目实测验证过：把 `startKickMs` 从 150 加到 300、`speedFast` 从 150 加到 200 之后，车反而连空载都不转了。

### 9.4 解法（比赛前必须做）

```
电池盒 4×AA (6V) 正极 ──> MX1508 的 VM
电池盒 负极 ──┬────────> MX1508 的 GND
              └────────> Arduino 的 GND     ← 必须共地！
Arduino ──USB──> 电脑 / 充电宝                  ← 只给板子供电
```

**电机的电走电池，板子的电走 USB，两边共地。** 改完之后：

- 起步电流随便给，`startKickMs` 可改回 **100**
- `speedFast` 用 **130** 起步试，稳了再加
- `speedTurn` / `speedBack` 可以加到 200，转向又狠又稳
- 保险丝不再受冲击，不会越用越虚

### 9.5 另一个绑定关系

只要电机还挂在 Uno 的保险丝后面，这两件事只能二选一：

- **要转向狠**（内侧轮反转）→ 换向是电流冲击最大的动作 → 中途可能停
- **要不停车**（内侧轮不反转）→ 转向明显变弱

接上电池之后就不用二选一了。

---

## 十、分步测试程序 `src/bringup.cpp`

装车之前先用它把每个部件单独试一遍。

```bash
pio run -e bringup -t upload
pio device monitor
```

串口输入命令（**大部分测试前先把车架起来**，但测电机负载时要放地面上）：

| 命令 | 功能 |
| --- | --- |
| `m` / `n` | 两个轮子一起前进 / 后退（带起步踢一脚） |
| `q` / `w` | 左转 / 右转（看差速方向对不对） |
| `l` / `h` | 把速度设成 100 / 220 |
| `s` | 停车 |
| **`f`** | **循迹测试模式**：直接按循迹逻辑跑，不做倒计时、不做起始线检测。用来在赛道上反复调参数 |
| `r` | 打印 20 组读数，带 `B`/`W` 标记，用来定阈值和验证黑白极性 |
| `b` | 打印启动键状态（0 = 没按，1 = 按下） |
| `o` | OLED 显示测试 |
| `t` | 蜂鸣器 + 两个指示灯测试 |
| `z` | **蜂鸣器引脚扫描**：从 D2 到 D13 挨个输出方波，用耳朵找出蜂鸣器实际接在哪个脚 |

**建议测试顺序**（对应 PPT 第 18 页）：

1. **OLED** —— `o`
2. **按键** —— `b`
3. **蜂鸣器 + LED** —— `t`
4. **电机**（架起车）—— `l` `m` `n` `q` `w` `s`
5. **光电检测** —— `r`，定阈值
6. **循迹**（放赛道上）—— `f`

---

## 十一、整机调试流程

1. **烧 bringup**，按第十节逐项验完
2. **烧正式程序** `pio run -e uno -t upload`
3. **把车摆在发车区**（离起始线 20~30cm，对准方向）
4. **按启动键** → 倒计时 → 发车
5. 观察并对照下表调整：

| 现象 | 原因 | 改什么 |
| --- | --- | --- |
| 发车 5 秒超时停车 | 车偏出走不到起始线 | 调 `trimLeft`；或把车摆正 |
| 起步不转、要推 | 供电带不动堵转电流 | 见第九节 |
| 转向时中途停住 | 换向电流冲击 | 见 9.5；或改电池供电 |
| 跑完一圈停不下来 | `startLineMs` 偏大 | 80 → 60 |
| 弯道提前停车 | `startLineMs` 偏小 | 80 → 120 |
| 直线上画龙 | 转向过头 | `speedBack` 60 → 40，`speedTurn` 135 → 125 |
| 转弯冲出去 | 转向不够 | `speedBack` 60 → 90，`speedTurn` 135 → 150 |
| 一直往一边偏 | 左右轮转速不一致 | 调 `trimLeft` |
| 压到线也不拐回来 | 先看屏幕上的调试信息 | 见 11.1，不用插电脑 |

> **一次只改一个参数**，改完跑 3 次再决定下一步。

### 11.1 跑完之后怎么"验尸"（`debugOled = 1`）

小车没法端着电脑跑，所以调试信息直接显示在 OLED 上。**跑的过程中**，计时下面是实时读数：

```
Running
00:12.34
L=920 R=760 LB        ← 左传感器 920(黑) 右传感器 760(白) → 程序判左转
[========        ]    ← 3 分钟进度条
```

**跑完（或冲出去撞停 / 超时）之后**，成绩画面会多出三行：

```
FINISHED!  time:
00:12.34
L 705-949             ← 左传感器这一趟读到过的【最小-最大】读数
R 710-762             ← 右传感器这一趟读到过的【最小-最大】读数
W120/L8/R0/B0         ← 四种情况各采样到多少次
```

第三行每 100 毫秒采样一次：`W`=两个都白(直行) `L`=左黑(左转) `R`=右黑(右转) `B`=双黑(起始线)。

> 开着 `debugOled` 时，成绩画面会**停 20 秒**，方便你走过去看；按一下启动键可以提前结束回到待机画面。

**怎么读**（阈值 `threshold = 840`，读数 **大于 840 算黑线**）：

| 屏幕显示 | 含义 | 下一步 |
| --- | --- | --- |
| `R 710-762`（最大值没超过 840） | **右传感器整趟都没见过黑线** —— 探头太高 / 线不够黑 / 线太细 | 用 `r` 重新测阈值，或降低探头高度 |
| `L 705-949` 且 `R 710-949` | 两个传感器都正常看到了黑线 | 传感器没问题，是控制/供电的问题 |
| `W` 很大、`L`/`R` 都是 0 | 车根本没压到线（方向偏了或者发车位置不对） | 调 `trimLeft`，或把车摆正 |
| `L` 和 `R` 都很大且来回跳 | 转向过冲、在画龙 | 降 `speedBack`、降 `speedFast` |
| `L` 大 `R` 一直是 0 | 只往一边偏（右轮偏慢） | 调 `trimLeft` |
| 有 `L`/`R` 但车不转 | 指令发了电机没执行 → 换向时供电塌了 / 某个通道反转不通 | 见第九节；用 bringup 的 `q` `w` 单独验反转 |

> 调试完把 `debugOled` 改回 `0`，成绩画面就会恢复成干净的"FINISHED + 时间 + press START again"。

---

## 十二、文件说明

```
lec1/
├── platformio.ini          编译配置（uno = 正式程序，bringup = 分步测试）
├── README.md               本文档（技术报告）
├── src/
│   ├── main.cpp            【正式程序】485 行，只有 setup() 和 loop()
│   └── bringup.cpp         【分步测试】488 行，串口发字母逐个测部件
├── test/                   之前小作业的参考程序，不参与编译
│   ├── Blink.cpp press.cpp light1.cpp light2.cpp ADD.cpp vmap.cpp oled.cpp
│   ├── 光电循迹小车设计与制作2024.pptx    课程 PPT
│   └── 作业提交文件/                       小作业 #1~#5 文档
└── docs/
    ├── hardware-schematic.png   PPT 第 19 页整机原理图
    ├── ir-sensor-circuit.png    PPT 第 13 页光电检测电路
    ├── ir-sensor-module.jpg     TCRT5000 模块实物
    └── oled-button-wiring.svg   OLED + 按键接线图
```

> `src/` 下两个 .cpp 都定义了 `setup()` 和 `loop()`，不能同时编译。
> `platformio.ini` 里用 `build_src_filter` 把它们分到两个环境。

---

## 十三、代码风格说明

本程序刻意保持简单，只用最基础的语法：

- **只有 `setup()` 和 `loop()` 两个函数**，没有自定义函数
- 不用数组、结构体、类、指针、枚举、lambda（Adafruit 的 `oled` 对象是库要求的）
- 只用 `int`、`unsigned long`、`if`、简单四则运算
- 只用必要库：`Adafruit_SSD1306` + `Adafruit_GFX`
- 按键消抖用最简单的 `delay(30)` 复检一次，不引入额外库

---

## 十四、常见问题排查

| 现象 | 可能原因 |
| --- | --- |
| OLED 全黑 | 地址不对（试 `0x3D`）；SDA/SCL 接反；**字符串没用 `F()` 导致显存分配失败**（见 7.1） |
| 串口发字母没反应 | 烧的是正式程序，不是 `bringup`；或串口被别的程序占用 |
| 小车原地打转 | 左右传感器或左右电机接反（见 3.1） |
| 走不了直线、一直往一边拐 | **传感器是"夹住线"摆法，"两个都白"必须按"车正"处理**（见 4.1） |
| 第一个直角弯就停车 | `startLineMs` 太小 |
| 到起始线不停 | `startLineMs` 太大；或起始线太窄没同时盖住两个传感器 |
| 起步不转 / 推一下才走 | 供电带不动堵转电流（见第九节） |
| 跑一会儿突然停 | 保险丝热了，电压塌下去（见 9.2） |
| 白底黑线读数差不多 | 收发管之间没隔光；或 R2 取值不合适 |
| 电机不转但屏幕正常 | 电压卡在 3.5~4V：单片机够用、电机不够（见 9.1） |
| 蜂鸣器不响 | 引脚不是 D4，用 `bringup` 的 `z` 命令扫描确认 |
