# HANDOFF —— 光电循迹小车项目交接说明

> 工作目录：`/Users/a27/Documents/PlatformIO/Projects/lec1`
> 技术报告全文见 `README.md`（526 行）。本文件只讲**当前状态、踩过的坑、待办**。

---

## 一、任务

《Arduino 编程及应用开发》课程大作业。PPT 要求：

> 按下启动按键 → 小车从发车区出发 → 经过起始线 → 沿黑线自主行走 **1 圈（3 分钟内）**
> → 再次到达起始线自动停下 → **OLED 显示运行时间**。

评分点（PPT 第 3 页）：过程 10% / 作品验收 40%（循迹稳定 10、自动起停 5、显示 5、按键 5、发挥 5）/ 技术报告 10%。

---

## 二、当前状态

### ✅ 已完成并验证

| 项 | 状态 |
| --- | --- |
| OLED 显示 | ✅ 实测通过（`o` 命令） |
| 按键 | ✅ 实测通过（`b` 命令） |
| 蜂鸣器 + LED | ✅ 实测通过（`t` 命令），引脚扫描确认在 **D4** |
| 电机正反转 / 转向 | ✅ 实测通过（`m` `n` `q` `w` `s`） |
| 左右映射 | ✅ 实测修正（丝印标的和实际是反的，见第五节） |
| 传感器黑白阈值 | ✅ 实测标定（`r` 命令），阈值 840 |
| 循迹逻辑 | ✅ 赛道实测，**完整跑通过一圈** |
| 编译 | ✅ 两个环境零警告零报错 |

| 程序 | Flash | RAM |
| --- | --- | --- |
| `src/main.cpp` | 56.9%（18360 / 32256） | 26.6%（544 / 2048） |
| `src/bringup.cpp` | 57.1%（18420 / 32256） | 26.6%（544 / 2048） |

### ❌ 未解决：供电

**电机挂在 Arduino 的 500mA 自恢复保险丝后面。** 表现为：起步要手推、转向时偶尔中途停。

定量分析见 `README.md` 第九节。结论：

```
两个电机起步需要 ≈ 2.2A
USB 供电路径只给 ≈ 0.5A  →  电机端只有 ~1.1V  →  扭矩不够
```

**这是软件绕不过去的**。现在能跑，靠的是保险丝的热惯性窗口，随时可能关闭。

---

## 三、硬件实际方案

| 部件 | 说明 |
| --- | --- |
| 主控 | Arduino Uno |
| 扩展板 | Arduino 小车扩展板（板载 MX1508、蜂鸣器、按键、LED、传感器接口） |
| 驱动 | **MX1508**（不是 TB6612，早期文档写错过） |
| 传感器 | 红外光电对管 ×2，**模拟输出** |
| 显示 | 0.96" OLED，SSD1306，128×64，I2C，地址 0x3C |
| 供电 | USB（**待改**） |

### 引脚表（全部实测确认，`src/main.cpp` 第 22~42 行）

| 功能 | 引脚 |
| --- | --- |
| 左传感器 | A0 |
| 右传感器 | A1 |
| 左轮 前进 / 反转 | D5 / D6 |
| 右轮 前进 / 反转 | D9 / D10 |
| 蜂鸣器 | D4 |
| 启动按键 | D7 |
| 运行指示灯 | D11 |
| 状态指示灯 | D12 |
| OLED SDA / SCL | A4 / A5 |

12 个引脚零冲突。每个轮子的两个脚在同一个定时器上（Timer0 / Timer1），
`tone()` 只占 Timer2，不影响电机 PWM。

---

## 四、当前参数（`src/main.cpp` 第 44~78 行）

| 参数 | 值 | 说明 |
| --- | --- | --- |
| `threshold` | 840 | 实测：黑线 915~949，白底 705~762，取中间 |
| `blackHigh` | 1 | 黑线读数大 |
| `speedFast` | 130 | 直行 |
| `speedTurn` | 135 | 转弯外侧轮 |
| `speedBack` | 60 | 转弯内侧轮**反转** |
| `trimLeft` | 0 | 左右补偿，**还没调过** |
| `startKickMs` | 150 | 起步满速踢 150ms |
| `startLineMs` | 80 | 起始线确认时间（线宽 3cm） |
| `lapMinMs` | 5000 | 最短一圈保护 |
| `lapMaxMs` | 180000 | PPT 要求的 3 分钟 |
| `launchMaxMs` | 5000 | 发车找线超时 |
| `oledMs` | 100 | 屏幕刷新间隔 |
| `debugLaunch` | 1 | 发车阶段打印读数到串口，排错时 1 |
| `debugOled` | 1 | 把调试信息显示在 **OLED** 上（跑完停屏"验尸"），稳定后改回 0 |

> **优化期间这些值被反复调整过。**
> 不要再"加大力度"—— 实测证明往大调会让保险丝更快失效（见 README 9.3 的正反馈），
> 也会让小车在窄黑线上过冲（往小调：`speedTurn` 135、`speedBack` 60）。

---

## 五、踩过的坑（按排查难度排序）

### 1. ⭐ OLED 一片黑 —— RAM 不够，`malloc` 失败

`Adafruit_SSD1306::begin()` 第一行是 `malloc(1024)`（128×64 的显存）。Uno 只有 2048 字节 RAM。

`bringup.cpp` 原来静态占用 **1246 字节**，1246 + 1024 = 2270 > 2048 → **`malloc` 失败 → `begin()` 立刻返回 false，一句 I2C 命令都没发**。

**编译器不会报任何警告。** 症状是屏幕全黑或停在旧画面。

**解法**：所有字符串包 `F()` 放 Flash。1246 → **544**。

### 2. ⭐ "两个都白"被当成丢线 —— 走不了直线

传感器是**夹住线**的摆法（黑线在两个传感器中间），所以**车正的时候就是两个白**。
原代码把"两个都白"当成丢线并按上次方向继续转 → 车一直在拐。

**解法**：两个都白 = 车正 = **直行**。同时删掉了 `lastTurn` 丢线搜索 ——
在直道上车正时就是两个白，时间长短没法区分"在线上"和"丢了"。

### 3. ⭐ 左右全反 —— 丝印和实际不一致

扩展板丝印标的左右和实际是反的，而且是**传感器和电机两对都反**。
（两对都反本来会互相抵消、循迹反而是对的，但 bringup 里 `q`/`w`/`r` 的标签全是错的，调试时会被严重误导。）

**解法**：实测确认后按实测写死（`leftSensorPin = A0`、`leftForward = 5`）。
实测方法记在 README 3.1 节。

### 4. ⭐ 内侧轮"减速"反而是刹车 —— 转不过来

MX1508 用 `IN1=PWM、IN2=0` 驱动时，**PWM 低电平期间两个输入都是 0，H 桥把电机两端短接 → 电机变成刹车**。

所以"把内侧轮调慢"实际是"把内侧轮变成刹车"，越慢越转不过来。

**解法**：内侧轮改成**反转**（`speedBack`），外侧轮往前冲，车身几乎原地转。

### 5. 起步不转，要手推 —— 静摩擦 / 堵转电流

直流电机**静止时阻力比转动时大得多**，直接给 `speedFast` 经常原地不转。

**解法**：发车时先给满速 255 冲一下（`startKickMs`），再降回巡航速度。

### 6. 跑完一圈停不下来 —— `startLineMs` 和线宽不匹配

判圈要求"双黑连续保持 `startLineMs`"。起始线只有 **3cm**，压线时间 = 3cm ÷ 车速。

原来 `startLineMs = 150`，车跑 20cm/s 时压线刚好 150ms —— **卡在阈值上判不出来**。

**解法**：降到 80ms（容忍到 37cm/s）。

### 7. 发车 5 秒超时 —— 阻塞确认把线冲过去了

发车阶段原来是"看到双黑 → `delay(150)` → 再确认一次"。
车快的时候 150ms 已经冲过整条线，第二次读到白底 → 永远找不到起始线 → 5 秒超时。

**解法**：发车阶段看到双黑直接开始计时，不做延时确认。

### 8. `int` 溢出 —— Uno 上 `int` 只有 16 位

`int lapMaxMs = 180000;` 编译器报 `overflow`（`int` 上限 32767），3 分钟限时会失效。

**解法**：所有毫秒变量用 `unsigned long`。

### 9. `tone()` 能用在任意引脚

一开始把蜂鸣器猜在 D3，不响。查了 `Tone.cpp` 才发现：Uno 上 `tone()` 是**用 Timer2 中断软件翻转 PORT 寄存器**，
所以**任何数字脚都能用**。用 `bringup` 的 `z` 命令逐个脚扫描，实测在 **D4**。

---

## 六、下一步待办（按优先级）

1. **改供电（唯一真正重要的事）**
   ```
   电池盒 / 充电宝 ──> MX1508 的 VM
   Arduino ──USB──> 电脑 / 另一个充电宝
   两边 GND 接在一起
   ```
   改完之后可以：`startKickMs` 降到 100、`speedFast` 提到 180、`speedTurn`/`speedBack` 提到 200。

2. **调 `trimLeft`**（现在还是 0）—— 发车段是开环直冲，车偏了会压不到起始线。
   用 `bringup` 的 `f` 或 `m` 命令调，往左偏填正数，每次 ±5。

3. **补充材料**（还没做）：
   - 技术报告里"问题分析与解决"一节（第五节的九个坑可以直接用）
   - `docs/` 下补一张供电改接示意图
   - 小作业提交文件是否齐全

---

## 七、命令

```bash
export PATH="$HOME/.platformio/penv/bin:$PATH"     # pio 不在默认 PATH

pio run -e uno                   # 只编译正式程序
pio run -e uno -t upload         # 编译并烧录正式程序
pio run -e bringup -t upload     # 烧录分步测试程序
pio device monitor               # 串口监视器 9600
```

> VS Code 状态栏的 ✓ / → 按钮烧哪个环境，由 `platformio.ini` 的 `default_envs` 决定（当前 = `uno`），改完要重启 VS Code。

### `src/bringup.cpp` 命令表

| 命令 | 功能 |
| --- | --- |
| `m` / `n` | 前进 / 后退（带起步踢一脚） |
| `q` / `w` | 左转 / 右转 |
| `l` / `h` | 速度设 100 / 220 |
| `s` | 停车 |
| **`f`** | **循迹测试模式**（不走倒计时、不判起始线，直接在赛道上调参数） |
| `r` | 打印 20 组读数，带 `B`/`W` 标记 |
| `b` | 打印按键状态 |
| `o` | OLED 测试 |
| `t` | 蜂鸣器 + LED 测试 |
| `z` | 蜂鸣器引脚扫描（D2~D13） |

---

## 八、用户明确提出的代码风格约束

**继续改代码必须遵守，否则会被打回。** 用户是刚学 Arduino 的大学生：

1. 代码尽可能简单，优先最基础最好理解的写法
2. **除 `setup()` 和 `loop()` 外不定义其他函数**
3. **不用数组、结构体、类、指针、枚举、lambda**（Adafruit 的 `oled` 对象除外）
4. 不为"规范"把简单代码封装复杂
5. 主要只用 `int`、`unsigned long`、`digitalRead/Write`、`pinMode`、`delay`、`millis`、`if`、简单四则运算
6. 只用完成任务必要的库
7. OLED 用 `Adafruit_SSD1306` + `Adafruit_GFX`
8. **不用 Bounce2 等按键库**
9. 消抖用最简单的方法（现在是 `delay(30)` 复检）
10. 变量名简单直观
11. 不写很长的一行表达式
12. 每个可能没学过的语句加简短中文注释
13. 保证 Uno + PlatformIO 直接编译
14. 保留 `#include <Arduino.h>`
15. 给完整代码，不给片段
16. 简单解释运行过程，不讲太多高级原理
17. 优先"刚学 Arduino 的人自己能写出来"的版本
18. 能用更简单的方法就用更简单的

> 早期版本用过非阻塞状态机、比例控制、滤波、迟滞、EEPROM —— **全部被要求删掉了**，不要再加回去。

---

## 九、环境

- PlatformIO Core **6.2.0**，`~/.platformio/penv/bin/pio`
- `platform = atmelavr` / `board = uno` / `framework = arduino`
- `lib_deps`：`adafruit/Adafruit GFX Library`、`adafruit/Adafruit SSD1306`
- **不是 git 仓库**，没有版本控制。根目录有 `.backups/` 两个手工备份目录
- 文件沙箱：danger-full-access；审批提示已禁用

---

## 十、文件清单

```
lec1/
├── platformio.ini          两个环境：uno / bringup
├── README.md               【技术报告】526 行
├── HANDOFF.md              本文件
├── src/
│   ├── main.cpp            正式程序（427 行）
│   └── bringup.cpp         分步测试程序（341 行）
├── test/                   小作业参考程序 + PPT + 提交文档，不参与编译
│   └── README              已替换为说明文件
├── docs/
│   ├── hardware-schematic.png   PPT 第 19 页整机原理图
│   ├── ir-sensor-circuit.png    PPT 第 13 页光电检测电路
│   ├── ir-sensor-module.jpg     TCRT5000 模块实物
│   └── oled-button-wiring.svg   OLED + 按键接线图
└── .backups/               手工备份（可以删）
```

已删除：`inc/car_hw.h`、`test/test_bringup.cpp`、`src/test_bringup.cpp`、`compile_commands.json`
